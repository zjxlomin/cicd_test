name: Github Actions 실행시켜보기

on:
  push:
    branches:
      - main

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World 찍기
        run: echo "Hello World"

      - name: 여러 명령어 문장 작성하기
        run: |
          echo "Good"
          echo "Day"
          echo "Everybody"

      - name: Github Actions 자체 저장된 변수 사용하기
        run: |
          echo $GTIHUB_SHA
          echo $GTIHUB_REPOSITORY